Build the workspace:
npm install

Uses a JSON data file to supply initial data. Implemented to practice with JSON formats.
npm install -g json-server

Run file in a server:
json-server --watch data/db.json
